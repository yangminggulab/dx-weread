{
  "name": "yangminggu-tasks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync:dashboard": "mkdir -p public/tasks && cp dashboard.html public/tasks/index.html",
    "deploy": "npm run sync:dashboard && wrangler deploy",
    "dev": "npm run sync:dashboard && wrangler dev",
    "check:worker": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
