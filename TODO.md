# TODO

## 圆环进度条 >100% 交互修正

### 目标行为
苹果风格的"前缘推进"模型，不是"两段弧线叠加"。

### 层级关系
- **底层（永久）**：进度达到 100% 时形成完整闭合圆环，之后这一层不再变化
- **上层（动态）**：超出 100% 的部分，表现为一个"前缘"在底层圆环上方滑行

### 具体改动点

1. **100% 必须是绝对闭环**  
   不能让用户感觉到"起点在六点钟"，要是一个没有接缝的完整圆。

2. **>100% 改为层级模型**  
   不再把超出部分当成"还在增长的路径"，而是"完整底环 + 上层前缘推进"。

3. **前缘后面无缝融入底环**  
   前缘后面的弧线与底层圆环同色、同厚度、同曲率，完全贴合，看不出第二段的起笔。

4. **去掉底部的"第二个头"造型**  
   六点钟附近不能有第二个圆头、第二个阴影、第二个接缝提示。

5. **阴影只服务前缘**  
   前缘可以轻微加深或投影（暗示"压在最上面"），六点钟尾部不加任何阴影或独立结构。

### 验收标准
- 用户只能看见一个在动的前缘
- 六点钟位置没有任何第二个起点的视觉提示

### ⚠️ 未完成
圆环尾部（六点钟）问题还没改好，待继续处理。

---

## Claude Code & Codex Usage 监控面板

### 目标
做一个面板，统一监控 Claude Code 和 OpenAI Codex 的用量数据。

### 待定细节
- 数据来源（API 直接拉取 / 本地日志解析 / 两者结合）
- 展示维度：按日/周/月、按模型、按 token 类型（input/output/cache）
- 是否集成到现有这个 App，还是独立页面
- 成本换算（是否显示美元估算）

---

## 小程序 → macOS 小组件

### 目标
调研能不能把现有小程序的核心功能做成 macOS 原生小组件（Widget）。

### 待调研
- 技术路线：SwiftUI WidgetKit（原生）vs Tauri/Electron 方案
- 数据对接：小组件直接读同一套云端 API（Cloudflare Worker）
- 功能范围：展示今日任务进度 / 圆环 / 阅读时长，还是更多
- 交互限制：macOS widget 基本只读，点击只能跳转 App，确认是否接受

---

## 小程序统计数字配色

- 连续完成天数 → 红色
- 累积完成数字 → 紫色
